[phases.setup]
nixPkgs = ["ffmpeg", "dejavu_fonts", "freefont_ttf"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "gunicorn app:app --bind 0.0.0.0:$PORT --workers 1 --threads 4 --timeout 300"
