[phases.setup]
nixPkgs = ["ffmpeg", "python311"]
aptPkgs = ["ffmpeg", "ffprobe"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "gunicorn app:app --bind 0.0.0.0:$PORT --workers 1 --timeout 300 --log-level info"
